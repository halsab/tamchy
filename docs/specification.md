# «Тамчы»: первая версия обновления возрастных режимов и игровых механик

**Версия требований:** 2.0\
**Статус:** согласованная целевая спецификация; выполнена подготовка данных и исходников, режимы и механики ещё не реализованы\
**Дата актуализации:** 8 сентября 2026 года\
**Основа:** текущий MVP, целевая модель механик v2, итоговый реестр иллюстраций/SVG и итоговый сценарий озвучки\
**Область:** `Төсләр`, `Хайваннар`, `Саннар`

## Нормативные документы

Для этой итерации используются три уровня источников:

1. **этот документ** — режимы, механики, сложность, игровой поток и технический контракт реализации;
2. **[реестр иллюстраций](illustrations-registry.md)** — точный состав графических ресурсов и правила их использования;
3. **[сценарий озвучки](audio-recording-script.md)** — точные татарские формы, Clip ID, состав аудиоконструктора и объём записи.

Реестры уточняют графику, татарские формы и Clip ID. Они приведены к согласованным решениям: все 12 существующих интерактивных реплик сохраняются; базовый аудионабор v2 — 175 клипов конструктора и 12 реплик, всего 187. Из отдельного документа расширения палитры приняты точные ID, названия и HEX; его ограничения двух ответов, прежних механик и цельных заданий заменены настоящим ТЗ.

## Текущий этап

[Руководство по контенту](content-guide.md) разделяет доступные исходники, ожидаемые записи и ресурсы работающей игры. В `src/content/v2/` зарегистрированы 13 цветов, 53 графических мастера и 187 Clip ID с путями. В игру пока подключён [каталог MVP](specification-mvp.md): 4 цвета, 6 животных, числа 1–5 и 42 MP3. Новые данные не импортируются в действующую сессию и не объявляются готовым офлайн-набором. Переход к v2 выполняется поэтапно по разделу 21.

---

# 1. Граница обновления

В этой версии приложение сохраняет три существующих учебных раздела:

1. `Төсләр` — цвета;
2. `Хайваннар` — животные;
3. `Саннар` — числа.

Разделы `Әлифба` и `Табигать` в эту итерацию **не входят**.

Добавляются два возрастных режима:

- **младший режим:** ориентировочно 2–4 года;
- **старший режим:** ориентировочно 5–7 лет.

Возрастные диапазоны являются продуктовым ориентиром. Режим выбирает взрослый и может изменить его в разделе для взрослых. Выбор режима допускается сохранять как локальную настройку, но игровые результаты ребёнка не сохраняются.

В этой версии **ни одна игровая механика не требует умения читать**. Письменные названия животных и цветов, задания на буквы, первую букву слова, слоги и сопоставление изображения со словом отсутствуют.

Цифры в разделе `Саннар` являются непосредственно изучаемым материалом и не считаются механикой чтения.

Существующие требования MVP по PWA, офлайн-работе, доступности, отсутствию аккаунтов, аналитики, рекламы и серверного бэкенда сохраняются, если ниже явно не указано иное.

---

# 2. Объём учебного контента

## 2.1. Младший режим

| Раздел | Контент |
|---|---:|
| Цвета | 13 цветов |
| Животные | 39 животных |
| Числа | 1–10 |

## 2.2. Старший режим

| Раздел | Контент |
|---|---:|
| Цвета | те же 13 цветов |
| Животные | те же 39 животных |
| Числа | 1–20 для распознавания числа; визуальные количества преимущественно 1–10 |

Контент между режимами не дублируется. Один учебный объект используется в разных упражнениях.

## 2.3. Критическое ограничение по числам

Визуальная группа из 11–20 отдельных предметов **не используется как основная счётная задача на телефоне**.

Итоговое правило:

```text
1–10  → визуальный счёт, выбор группы, сравнение, сложение и вычитание
1–20  → распознавание числа по озвучке и выбор цифры
```

N2 и N3 используют небольшие визуальные количества, преимущественно в диапазоне 1–10.

---

# 3. Младший режим

Младший режим сохраняет один смысловой принцип:

**услышать → найти правильный объект.**

Новые мыслительные типы упражнений в младший режим не добавляются. Разные формулировки одного и того же задания не считаются отдельными игровыми механиками.

Меняются:

- объём контента;
- количество вариантов ответа;
- адаптивная сложность текущей сессии;
- набор допустимых аудиоформулировок.

## 3.1. Количество вариантов

Возможные состояния:

```text
2 → 3 → 4
```

Новая сессия начинается с двух вариантов.

При устойчивых правильных ответах количество вариантов постепенно увеличивается.

При серии ошибок количество вариантов уменьшается:

```text
4 → 3 → 2
```

Одиночная ошибка не должна автоматически снижать сложность.

Ребёнку не показываются:

- уровни;
- серии правильных ответов;
- повышение или понижение сложности;
- очки;
- игровой прогресс.

Адаптация существует только внутри текущей сессии.

Точные пороги переключения `2 ↔ 3 ↔ 4` остаются параметрами балансировки и не фиксируются настоящим обновлением.

## 3.2. Прямые задания младшего режима

### Цвета

Используется семантика C1: ребёнок слышит название цвета и выбирает нужный цвет.

Допустимые аудиоварианты:

```text
{color} + colors.findTail
{color} + colors.chooseTail
{color} + colors.showTail
```

Примеры:

```text
Кызыл төсне тап.
Кишер сары төсне сайла.
Күк зәңгәр төсне күрсәт.
```

### Животные

Для каждого животного используются все пять вариантов формулировки:

```text
{animal} + common.where
{animal.target} + common.find
{animal.target} + common.choose
{animal.target} + common.show
common.which + {animal}
```

Например:

```text
Аю кайда?
Аюны тап.
Аюны сайла.
Аюны күрсәт.
Кайсысы аю?
```

### Числа

В младшем режиме используются числа 1–10. Задания остаются прямыми и визуально понятными. Визуальное количество не превышает 10.

---

# 4. Старший режим

Старший режим использует ту же базу контента, но задания требуют разных операций:

- распознавания;
- учёта нескольких визуальных признаков;
- сравнения;
- определения закономерности;
- количественного сопоставления;
- визуальной арифметики.

Чтение не требуется.

---

# 5. Модель сложности старшего режима

Используются две независимые оси сложности:

1. **тип упражнения**;
2. **количество вариантов ответа**, если механика допускает множественный выбор.

## 5.1. Уровни упражнений

### L1 — прямое распознавание

Ребёнок определяет непосредственно наблюдаемый объект или признак.

### L2 — сопоставление или сравнение

Для ответа необходимо учитывать несколько признаков или сравнить объекты.

### L3 — вывод

Требуется определить закономерность или выполнить простую количественную операцию.

Уровни ребёнку не показываются.

## 5.2. Разблокировка уровней

Новая сессия начинается с L1.

Для первой реализации:

```text
0–2 правильных ответа → только L1
3+ правильных         → доступны L1 и L2
8+ правильных         → доступны L1, L2 и L3
```

После разблокировки более простые задания не исчезают.

При полном наборе уровней ориентировочное распределение:

```text
L1 — 40%
L2 — 35%
L3 — 25%
```

Неправильный ответ не уменьшает уже накопленный `correctCount`.

При серии затруднений генератор может временно чаще выбирать простые уже доступные задания.

---

# 6. Адаптация количества вариантов старшего режима

Для обычных заданий с множественным выбором:

```text
4 → 5 → 6
```

Новая сессия начинается с четырёх вариантов.

Для первой реализации:

- 4 правильных ответа подряд → `4 → 5`;
- ещё 5 правильных подряд → `5 → 6`;
- 2 ошибки подряд → понижение на одну ступень;
- единичная ошибка не понижает число вариантов;
- после изменения ступени счётчики серии сбрасываются.

Количество вариантов и уровень упражнения изменяются независимо.

Возможны, например:

- L1 с 6 вариантами;
- L3 с 4 вариантами.

Для механик, структура которых естественно предполагает два сравниваемых объекта, количество вариантов до 4–6 искусственно не увеличивается.

---

# 7. Раздел `Төсләр`

Используются четыре механики C1–C4.

## 7.1. Утверждённая палитра — 13 цветов

| ID | Татарское название | HEX |
|---|---|---|
| `red` | Кызыл | `#D94343` |
| `orange` | Кишер сары | `#F28C28` |
| `yellow` | Сары | `#F3C63A` |
| `green` | Яшел | `#32965B` |
| `turquoise` | Фирәзә | `#2CAFA5` |
| `light-blue` | Күк зәңгәр | `#4AA8E8` |
| `blue` | Зәңгәр | `#347FD4` |
| `purple` | Шәмәхә | `#7959C8` |
| `pink` | Ал | `#E66D9A` |
| `brown` | Көрән | `#8A5A3B` |
| `gray` | Соры | `#7D8583` |
| `white` | Ак | `#FFFFFF` |
| `black` | Кара | `#222625` |

Точные HEX являются учебными данными и не меняются ради оформления.

Для белого `#FFFFFF` контраст обеспечивается рамкой, поверхностью карточки или другим одинаковым для всех цветов оформлением; сам учебный цвет не изменяется.

---

## C1. Найди названный цвет

**Уровень:** L1\
**Ответы:** 4–6 в старшем режиме; 2–4 в младшем.

### Задача

Ребёнок слышит название цвета и выбирает соответствующую цветовую фишку.

Аудио случайно собирается по одному из трёх шаблонов:

```text
{color} + colors.findTail
{color} + colors.chooseTail
{color} + colors.showTail
```

Примеры:

```text
Кызыл төсне тап.
Кишер сары төсне сайла.
Күк зәңгәр төсне күрсәт.
```

### Требования

- правильный ответ ровно один;
- цвета вариантов не повторяются;
- позиция правильного ответа перемешивается;
- форма, размер и оформление фишек одинаковы;
- отличие определяется только учебным цветом;
- нельзя подсказывать ответ размером, положением или дополнительным декором.

---

## C2. Определи цвет предмета

**Уровень:** L1\
**Ответы:** 4–6 цветовых фишек.

### Задача

На экране показывается один крупный предмет заданного учебного цвета. Ребёнок определяет цвет и выбирает соответствующую фишку.

Фиксированное аудио:

```text
Бу нинди төс?
```

Clip ID:

```text
colors.objectColor.prompt
```

### Утверждённый набор нейтральных SVG-предметов

```text
color-object-ball.svg
color-object-ring-toy.svg
color-object-car-toy.svg
```

Дополнительно в механике могут использоваться подходящие геометрические формы из утверждённого набора 6 SVG.

Не используются:

```text
color-object-cube.svg
color-object-pyramid-toy.svg
color-object-top.svg
```

### Требования

- один SVG работает со всеми 13 учебными цветами;
- основной цвет задаётся программно через `currentColor`;
- дополнительные детали допустимы только тем же `currentColor` с прозрачностью;
- нельзя вводить второй фиксированный цвет, меняющий учебный смысл;
- объект не должен иметь сильной естественной ассоциации с конкретным цветом;
- объект целиком находится в `viewBox` и хорошо читается на мобильном экране.

---

## C3. Составной визуальный признак

**Уровень:** L2\
**Ответы:** 4–6.

В финальной аудио- и ресурсной схеме C3 имеет два варианта.

### C3-A. Цвет + форма

Используются 6 форм:

```text
Түгәрәк
Шакмак
Өчпочмак
Турыпочмаклык
Овал
Йолдыз
```

Шаблоны аудио:

```text
{color} + {shape.target} + common.find
{color} + {shape.target} + common.choose
{color} + {shape.target} + common.show
```

Примеры:

```text
Кызыл шакмакны тап.
Яшел өчпочмакны сайла.
Күк зәңгәр йолдызны күрсәт.
```

### C3-B. Размер + цвет + форма

Размеры:

```text
Зур
Кечкенә
```

Шаблоны аудио:

```text
{size} + {color} + {shape.target} + common.find
{size} + {color} + {shape.target} + common.choose
{size} + {color} + {shape.target} + common.show
```

Примеры:

```text
Зур кызыл түгәрәкне тап.
Кечкенә яшел шакмакны сайла.
Зур шәмәхә йолдызны күрсәт.
```

### Правила генератора C3

- ровно один вариант соответствует всем названным признакам;
- обязательно присутствуют правдоподобные помехи, совпадающие только по части признаков;
- размер задаётся интерфейсом, отдельные SVG для большой и маленькой версии не создаются;
- отдельные изображения комбинаций `цвет × форма × размер` не создаются.

Количество как дополнительный признак в C3 этой итерации не используется.

---

## C4. Продолжи цветовую закономерность

**Уровень:** L3\
**Ответы:** 4–6.

### Задача

Показывается короткая последовательность цветов и пустая следующая позиция.

Фиксированное аудио:

```text
Киләсе төсне тап.
```

Clip ID:

```text
colors.sequence.prompt
```

Поддерживаемые шаблоны:

```text
A B A B ?
A A B A A B ?
A B C A B C ?
```

Ограничения:

- используется только поиск следующего элемента;
- внутренние пропуски пока не используются;
- последовательность короткая;
- правильный ответ единственный.

---

# 8. Раздел `Хайваннар`

Используются три механики A1–A3.

Общая база — **39 животных**. Между возрастными режимами изображения не дублируются.

## A1. Найди названное животное

**Уровень:** L1\
**Ответы:** 4–6 в старшем режиме; 2–4 в младшем.

### Задача и аудио

Для каждого животного используются:

- обычная форма `animal.*`;
- объектная форма `animal.*.target`;
- пять вариантов инструкции.

Шаблоны:

```text
{animal} + common.where
{animal.target} + common.find
{animal.target} + common.choose
{animal.target} + common.show
common.which + {animal}
```

Например:

```text
Аю кайда?
Аюны тап.
Аюны сайла.
Аюны күрсәт.
Кайсысы аю?
```

### Требования

- правильное животное одно;
- варианты уникальны;
- изображение животного показывается целиком;
- позиция правильного ответа случайна;
- помехи — другие животные каталога;
- цель не должна систематически повторяться подряд.

---

## A2. Найди животное по признаку

**Уровень:** L2\
**Ответы:** 4–6.

### Утверждённые признаки и аудио

В этой итерации используются только заранее проверенные признаки:

| Trait ID | Clip ID | Аудио |
|---|---|---|
| `bird` | `animals.trait.bird` | `Кошны тап.` |
| `domestic` | `animals.trait.domestic` | `Йорт хайванын тап.` |
| `wild` | `animals.trait.wild` | `Кыргый хайванны тап.` |
| `canFly` | `animals.trait.canFly` | `Кайсысы оча?` |
| `canSwim` | `animals.trait.canSwim` | `Кайсысы йөзә?` |

### Контентная модель

Приложение не выводит признаки из изображения или названия. Для каждого животного хранятся явно проверенные `traits`.

В каждом сгенерированном наборе **ровно одно показанное животное** должно удовлетворять вопросу.

Не использовать неоднозначные, спорные или контекстно зависимые признаки.

---

## A3. Узнай животное по силуэту

**Уровень:** L2\
**Ответы:** 4–6.

Фиксированное аудио:

```text
Бу кайсы хайванның шәүләсе?
```

Clip ID:

```text
animals.silhouette.prompt
```

### Создание силуэта

Отдельные силуэтные изображения не создаются.

Силуэт формируется локально из альфа-канала существующей иллюстрации животного, например через CSS-mask / `-webkit-mask` или эквивалентную локальную реализацию.

### Ограничение конфликтов

Необходимо вручную проверить все 39 иллюстраций и зафиксировать пары/группы, которые нельзя показывать одновременно из-за слишком похожего контура.

Контентная модель должна поддерживать явный список конфликтов.

Генератор обязан гарантировать:

- силуэт соответствует одному из ответов;
- правильный ответ один;
- конфликтующие силуэты не появляются вместе;
- варианты уникальны;
- позиция правильного ответа случайна.

---

# 9. Раздел `Саннар`

Используются три механики N1–N3.

Ключевой принцип: числа 11–20 используются для распознавания числа, но не для больших визуальных групп предметов.

## 9.1. Пул считаемых объектов — 10 типов

Внутри одного задания используется только **один тип объекта**.

```text
Алма
Түгәрәк
Шакмак
Өчпочмак
Турыпочмаклык
Овал
Йолдыз
Туп
Боҗра
Машина
```

Ресурсы:

- `count-apple.png`;
- 6 геометрических SVG;
- `color-object-ball.svg`;
- `color-object-ring-toy.svg`;
- `color-object-car-toy.svg`.

---

## N1. Число и количество

**Уровень:** L1

N1 состоит из трёх форм прямого распознавания.

### N1-A. Озвученное число → выбрать цифру — 1–20

Используются пять вариантов:

```text
{number} + common.where
{number.target} + common.find
{number.target} + common.choose
{number.target} + common.show
common.which + {number}
```

Пример для 17:

```text
Унҗиде кайда?
Унҗидене тап.
Унҗидене сайла.
Унҗидене күрсәт.
Кайсысы унҗиде?
```

Диапазон:

```text
1–20
```

### N1-B. Количество → выбрать цифру — 1–10

Показывается группа одинаковых объектов. Ответами служат цифры.

Аудио:

```text
common.howMany + {countObject} + common.has
common.countHowMany + {countObject} + common.has
```

Примеры:

```text
Ничә алма бар?
Ничә туп бар?
Санап кара. Ничә боҗра бар?
```

Простое `Ничә?` без указания объекта **не используется**.

### N1-C. Показанная цифра → выбрать группу — 1–10

Показывается цифра. Ответами служат группы одинаковых предметов.

Аудио:

```text
common.whichIn + {number} + {countObject} + common.has
{number} + {countObject} + common.with + common.find
{number} + {countObject} + common.with + common.choose
{number} + {countObject} + common.with + common.show
```

Примеры:

```text
Кайсысында биш туп бар?
Биш туп булганын тап.
Биш туп булганын сайла.
Биш туп булганын күрсәт.
```

### Помехи

Для числовых ответов предпочтительны близкие значения, но все варианты должны быть уникальны и находиться в допустимом диапазоне конкретной подмеханики.

### Количество вариантов

В старшем режиме применяется общая адаптация 4–6, если компоновка задания остаётся читаемой и однозначной.

---

## N2. Где больше / где меньше

**Уровень:** L2\
**Ответы:** всегда 2.

На экране две группы **одинакового типа объекта**.

Аудио:

```text
Кайсысында күбрәк?
Кайсысында азрак?
```

Clip ID:

```text
numbers.more.prompt
numbers.less.prompt
```

Сложность регулируется близостью сравниваемых значений, а не числом карточек.

Визуальные количества преимущественно находятся в пределах 1–10.

---

## N3. Визуальное сложение и вычитание

**Уровень:** L3\
**Ответы:** 4–6, если это не ухудшает читаемость.

Используется тот же пул из 10 считаемых объектов.

### N3-A. Сложение

Показываются две группы одинаковых объектов и их визуальное объединение.

Результат сложения не больше 10.

Аудио:

```text
common.totalHowMany + {countObject}
common.countTotalHowMany + {countObject}
```

Примеры:

```text
Барысы ничә алма?
Барысы ничә туп?
Санап кара. Барысы ничә боҗра?
```

### N3-B. Вычитание

Показывается исходная группа; часть предметов визуально обозначается как убранная.

Аудио:

```text
common.howMany + {countObject} + common.left
common.countHowMany + {countObject} + common.left
```

Примеры:

```text
Ничә алма калды?
Ничә машина калды?
Санап кара. Ничә йолдыз калды?
```

### Ограничения N3

- результат сложения `≤ 10`;
- отрицательные результаты запрещены;
- результат `0` не используется, пока ноль отдельно не введён;
- внутри одного задания все считаемые объекты одного типа;
- количество формируется кодом;
- отдельные готовые картинки арифметических примеров не создаются;
- письменные выражения `3 + 2 = ?` в этой итерации не требуются.

---

# 10. Общая таблица механик старшего режима

| ID | Раздел | Механика | Уровень | Ответы / диапазон |
|---|---|---|---|---|
| C1 | Цвета | Найди названный цвет | L1 | 4–6 |
| C2 | Цвета | Определи цвет предмета | L1 | 4–6 |
| C3 | Цвета | Цвет + форма / размер + цвет + форма | L2 | 4–6 |
| C4 | Цвета | Продолжи закономерность | L3 | 4–6 |
| A1 | Животные | Найди названное животное | L1 | 4–6 |
| A2 | Животные | Найди по признаку | L2 | 4–6 |
| A3 | Животные | Узнай по силуэту | L2 | 4–6 |
| N1 | Числа | Распознавание числа и количества | L1 | цифры 1–20; визуальный счёт 1–10 |
| N2 | Числа | Где больше / меньше | L2 | 2 |
| N3 | Числа | Визуальное сложение / вычитание | L3 | результат ≤ 10 |

Итого:

- **4 механики цветов**;
- **3 механики животных**;
- **3 механики чисел**;
- **10 игровых механик суммарно**.

---

# 11. Формирование игрового потока

Ребёнок **не выбирает конкретную механику**.

На главном экране выбирается только раздел:

```text
Төсләр
Хайваннар
Саннар
```

После входа начинается единая игровая сессия.

Генератор:

1. определяет доступные уровни сложности;
2. выбирает допустимый `ExerciseKind`;
3. генерирует конкретное задание;
4. выбирает допустимые помехи;
5. перемешивает позиции;
6. строит аудиоподсказку как один клип или последовательность клипов;
7. передаёт готовое упражнение игровому экрану.

Новые механики не создают отдельного дерева меню.

---

# 12. Общие требования генератора

Любая механика обязана гарантировать:

1. ровно один правильный ответ;
2. отсутствие дубликатов вариантов;
3. отсутствие случайной визуальной подсказки;
4. соответствие вариантов типу упражнения;
5. соответствие доступной сложности;
6. отсутствие длительной серии одного `ExerciseKind`;
7. отсутствие постоянной позиции правильного ответа;
8. воспроизводимую случайность через внедряемый генератор;
9. отсутствие ссылок на несуществующие графические или аудиоресурсы;
10. соблюдение диапазонов конкретной механики.

Дополнительно:

### C3

- правильный вариант соответствует всем указанным признакам;
- помехи совпадают только по части признаков.

### A2

- ровно один показанный вариант удовлетворяет запрошенному trait.

### A3

- конфликтующие силуэты не показываются вместе.

### N1

- визуальное количество не превышает 10;
- распознавание цифры по озвучке допускает 1–20.

### N2

- обе группы используют один тип объекта;
- количества различны;
- правильный ответ единственный.

### N3

- арифметика корректна;
- сумма не больше 10;
- разность положительна;
- все предметы одного типа.

---

# 13. Обратная связь и подтверждение ответа

Сохраняется базовая модель MVP.

## 13.1. Правильный ответ

- принимается только первое допустимое нажатие;
- выбранный вариант получает положительное визуальное подтверждение;
- ввод временно блокируется;
- затем проигрывается учебное значение, если это уместно;
- после реакции начинается следующее задание.

Используются существующие реплики похвалы из `interaction/` по разделу 13.5. Повторная запись этих файлов не требуется; дополнительные реплики добавляются только после анализа игрового потока.

Для подтверждения используются:

```text
цвет      → color.*
животное  → animal.*
число     → number.*
```

Например:

```text
Кызыл
Аю
Җиде
```

Для C3 допускается только визуальное подтверждение, если составная подтверждающая фраза звучит неестественно.

## 13.2. Неправильный ответ

- нет красного экрана;
- нет неприятного звука;
- нет очков или потерь;
- текущее упражнение сохраняется;
- ребёнок может попробовать снова.

Наказания и отрицательные оценки не используются. Существующие мягкие реплики повторной попытки сохраняются по разделу 13.5.

## 13.3. Повтор

Если задание имеет аудиоподсказку, повтор прерывает текущую речь и воспроизводит текущую подсказку заново. Запросы аудио не ставятся в очередь.

## 13.4. Выход

Возврат домой остаётся доступен. Выход отменяет звук, таймеры и устаревшие асинхронные действия текущей сессии.

---

## 13.5. Интерактивные реплики

Все 12 записей `interaction/` сохраняются в обоих возрастных режимах. Базовая политика общая для трёх разделов; раундом считается одно упражнение любого вида:

| Действие | Реплика и последовательность |
|---|---|
| Первый вход в раздел после открытия приложения | `hello` → задание; на главном экране автозвука нет |
| Следующий вход в любой раздел | `game-start` → задание |
| Правильный ответ | В нечётных раундах по кругу `correct`, `well-done`, `very-good` → название; в чётных — только название |
| Ошибка в текущем раунде | После 250 мс: первая — `think-again`, вторая — `hint`, третья — `try-again`, затем без реплики; после каждой снова полное задание |
| Единственное напоминание через 10 секунд | `listen` → задание |
| Каждый пятый новый раунд | `next-one` → задание |
| Продолжение после паузы до ответа | `continue` → исходное задание; после принятого ответа сохраняется один переход без повторного подтверждения |
| Кнопка повтора | Только исходное задание, с отменой текущей реплики |
| Домик из игры | Немедленный переход домой и `goodbye`; браузерная навигация и экран взрослых не произносят прощание |

Вступительная реплика не разрешает ответы: необходим фактический старт учебного задания. Похвала не заменяет название; 300 мс отсчитываются после окончания названия, также сохраняются 1200 мс после ответа. Последовательность принадлежит одной отменяемой операции: повтор, выход, новая сессия и пауза не оставляют отложенного звука. Прощание тоже прерывается новым входом, уходом с главного экрана, скрытием страницы и системным прерыванием; оно не задерживает навигацию.

В релизе v2 обязательны все 187 MP3: 175 клипов конструктора и 12 интерактивных реплик. Полная составная подсказка считается одной отменяемой операцией; недостающий обязательный клип блокирует задание. Во время игры ошибка загрузки/декодирования дополнительной реплики либо ожидание её загрузки более 2 секунд пропускает реплику и сохраняет учебную запись. Запрет звука и системное прерывание обрабатываются обычным восстановлением аудио. Ошибки обязательных учебных записей по-прежнему блокируют ответы до явного повтора. Прощание при ошибке пропускается, его ожидание ограничено 15 секундами.

Для C3 при визуальном подтверждении без учебной речи сохраняются уместная интерактивная похвала и минимум 1200 мс после ответа; при наличии похвалы также выдерживаются 300 мс после её окончания. Если учебное значение озвучивается, эти 300 мс отсчитываются после него. Дополнительные тексты реплик пока не утверждены: задача V2-05.

---

# 14. Итоговый графический комплект

## 14.1. Сводка мастер-ресурсов

| Группа | Количество |
|---|---:|
| Иллюстрации разделов | 3 PNG |
| Иллюстрации животных | 39 PNG |
| Предмет для счёта | 1 PNG |
| Мастер-иконка приложения | 1 PNG |
| Геометрические формы | 6 SVG |
| Нейтральные цветовые предметы | 3 SVG |
| **Всего** | **53 мастер-ресурса** |

Итого:

```text
44 PNG
9 SVG
```

Производные WebP, PWA-иконки и Apple touch icon не считаются отдельными мастер-ресурсами.

## 14.2. Структура исходников

```text
assets-source/
  categories/
    category-colors.png
    category-animals.png
    category-numbers.png

  animals/
    animal-*.png              # 39 файлов

  numbers/
    count-apple.png

  icons/
    app-icon-master.png

  svg/
    shapes/
      shape-circle.svg
      shape-square.svg
      shape-triangle.svg
      shape-rectangle.svg
      shape-oval.svg
      shape-star.svg

    color-objects/
      color-object-ball.svg
      color-object-ring-toy.svg
      color-object-car-toy.svg
```

## 14.3. Форматы поставки

- мастер-растры хранятся как PNG;
- в приложение поставляются оптимизированные WebP;
- программные формы и предметы остаются локальными SVG;
- иконки PWA создаются из `app-icon-master.png`.

## 14.4. Общие требования к 9 SVG

Все SVG:

- валидные и локальные;
- `viewBox="0 0 512 512"`;
- прозрачный фон;
- без `<image>` и base64-растра;
- без сетевых ссылок;
- без JavaScript;
- без текста;
- основной цвет через `currentColor`;
- допускаются прозрачные слои того же `currentColor`;
- должны работать со всеми 13 учебными цветами;
- объект не выходит за `viewBox`;
- ресурс остаётся читаемым на мобильном экране.

Чтобы `currentColor` реально передавался внутрь SVG, использовать inline-SVG или компонентный способ. Обычный `<img src="...">` не наследует `color` родительского элемента во внешний SVG.

## 14.5. Что не создаётся

Не создаются отдельные ресурсы для:

- 13 цветовых фишек;
- каждой комбинации `цвет × форма × размер`;
- 39 силуэтов животных;
- цифр 1–20;
- количеств 2, 3, 4 и т. д.;
- каждого арифметического примера.

Удалённые SVG:

```text
color-object-cube.svg
color-object-top.svg
color-object-pyramid-toy.svg
```

Они не входят в обязательный комплект сборки и офлайн-кэш.

---

# 15. Итоговая аудиосистема

## 15.1. Принцип

Не записывать все возможные предложения целиком.

Используется **аудиоконструктор** из самостоятельных слов и грамматически готовых словоформ.

Разрешено:

```text
Кызыл + төсне тап
Аюны + сайла
Кайсысы + аю
Унҗиде + кайда
Биш + туп + булганын + тап
Ничә + боҗра + бар
Зур + кызыл + шакмакны + күрсәт
```

Запрещено разрезать слово на корень и суффикс:

```text
аю + ны        # нельзя
мәче + не      # нельзя
```

Нужные формы записываются целиком:

```text
Аюны
Мәчене
Шакмакны
Унҗидене
```

## 15.2. Мастер-запись

Записываются 5 больших WAV-файлов:

```text
master-01-colors-shapes-objects.wav
master-02-numbers.wav
master-03-animal-labels.wav
master-04-animal-target-forms.wav
master-05-common-prompts.wav
```

После нарезки:

| Мастер | Клипов |
|---|---:|
| MASTER 01 — цвета / формы / предметы | 31 |
| MASTER 02 — числа | 40 |
| MASTER 03 — названия животных | 39 |
| MASTER 04 — объектные формы животных | 39 |
| MASTER 05 — общие части / фиксированные вопросы | 26 |
| **Всего** | **175** |

Итоговая схема:

```text
5 WAV-мастеров
↓
175 коротких клипов
↓
сотни конкретных фраз, собираемых приложением
```

Публикуемый формат коротких клипов — MP3 mono, ориентир 64–96 кбит/с. Исходные WAV хранятся в `assets-source/audio/tt/` отдельно от сборки. К 175 новым клипам добавляются 12 уже предоставленных интерактивных реплик: полный базовый реестр — 187. Точные пути всех файлов закреплены в `src/content/v2/audio.json` и [руководстве](content-guide.md).

## 15.3. Манера записи

Все части конструктора записываются:

- одним голосом;
- в одинаковом темпе;
- с одинаковой воспринимаемой громкостью;
- нейтрально и естественно;
- без слишком сильного финального падения интонации;
- с проверкой реальных склеек до массовой записи.

Если конкретная комбинация после склейки звучит неестественно, допускается отдельная цельная запись именно для неё.

## 15.4. Аудио, которое не требуется

Не записываются:

- все полные комбинации C1;
- 39 × 5 полных фраз животных;
- 20 × 5 полных фраз чисел;
- все комбинации цвета, формы и размера;
- все комбинации числа и считаемого объекта;
- все арифметические примеры;
- все цветовые последовательности;
- голоса животных;
- повторная запись уже предоставленной похвалы;
- наказание за ошибку;
- очки и уровни;
- `Әлифба`;
- `Табигать`;
- символические выражения `3 + 2 = ?`;
- `<`, `>`, `=`.

---

# 16. Технический контракт реализации

Базовый стек MVP сохраняется: React + TypeScript + Vite, локальный контент, Web Audio API, PWA и TDD.

## 16.1. Модель упражнения

Прежняя модель `targetId + два LearningItem` недостаточна.

Нужна общая сущность упражнения, например концептуально:

```ts
type Difficulty = 1 | 2 | 3;

type Exercise = {
  id: string;
  categoryId: CategoryId;
  kind: ExerciseKind;
  difficulty: Difficulty;
  prompt: Prompt;
  options: readonly Option[];
  correctOptionId: string;
};
```

Конкретные TypeScript-типы определяются реализацией, но должны быть дискриминированными объединениями, а не одним объектом с большим числом необязательных полей.

## 16.2. Представление вариантов

`Option` должен уметь представлять как минимум:

- цвет;
- растровое изображение;
- SVG-форму/предмет;
- число;
- группу считаемых объектов;
- составной объект `форма + цвет + размер`.

## 16.3. Представление аудиоподсказки

`promptAudio` больше не должен предполагать только один готовый MP3.

Нужна модель, поддерживающая:

```ts
type AudioPrompt =
  | { type: "clip"; clipId: AudioClipId }
  | { type: "sequence"; clipIds: readonly AudioClipId[] };
```

Это концептуальный контракт, а не обязательное имя типов.

Для пользователя последовательность клипов должна звучать как одна естественная фраза. Аудиосервис обязан корректно отменять всю последовательность при повторе, ответе, выходе или смене раунда.

## 16.4. Контентные сущности

Не дублировать одну сущность для каждой механики.

Нужны отдельные каталоги:

- цветов;
- животных;
- чисел;
- форм;
- размеров;
- считаемых объектов;
- аудиоклипов;
- проверенных traits животных;
- конфликтов силуэтов;
- правил генерации конкретных `ExerciseKind`.

## 16.5. Выбор режима

Состояние выбранного режима отделяется от игрового прогресса.

Допускается локально сохранять только:

```text
junior | senior
```

Счётчики правильных ответов, серии ошибок и текущая адаптивная сложность остаются состоянием текущей сессии.

## 16.6. Офлайн-кэш

Обязательный офлайн-набор этой итерации должен включать:

- производные WebP всех обязательных растровых учебных ресурсов;
- 9 SVG;
- 187 публикуемых MP3: 175 клипов конструктора и 12 существующих интерактивных реплик;
- PWA-иконки;
- код, стили, словари и остальные обязательные локальные ресурсы.

Старый числовой бюджет размера MVP нельзя автоматически считать подтверждённым для расширенного набора. После финального кодирования WebP/MP3 требуется повторно измерить полный обязательный пакет и либо подтвердить существующий бюджет, либо отдельно утвердить новый. Произвольное новое число в этом документе не фиксируется.

---

# 17. Автоматическая валидация и тесты

Существующий TDD-подход сохраняется.

## 17.1. Контентная валидация

Сборка должна завершаться ошибкой при:

- отсутствии одного из 13 цветов;
- изменённом HEX учебного цвета;
- отсутствии одного из 39 животных;
- отсутствии обязательного изображения;
- отсутствии одного из 9 SVG;
- наличии удалённых SVG в обязательном реестре;
- отсутствии обязательного Clip ID;
- ссылке аудиорецепта на неизвестный клип;
- дублирующемся ID;
- числе за пределами диапазона механики;
- визуальном количестве больше 10 там, где это запрещено;
- неверной арифметике;
- неоднозначной классификации A2;
- конфликтующей паре силуэтов A3;
- невозможности построить ровно один правильный ответ.

## 17.2. Обязательные тесты новых механик

### Общие

- ровно один правильный ответ;
- нет дубликатов;
- правильное число вариантов;
- детерминированность при фиксированной случайности;
- допустимый уровень сложности;
- корректная адаптация числа вариантов;
- отмена устаревшего аудио и таймеров.

### C1

- все 13 цветов могут становиться целью;
- точные HEX не изменяются;
- 4/5/6 уникальных цветов в старшем режиме.

### C2

- только утверждённые цветовые предметы/формы;
- любой из 13 цветов применяется программно;
- удалённые SVG не используются.

### C3

- корректные 6 форм;
- один правильный вариант по всем признакам;
- есть помехи, совпадающие только по части признаков.

### C4

- корректное продолжение всех разрешённых шаблонов;
- единственный правильный следующий цвет.

### A1

- все 39 животных могут становиться целью;
- работают все пять рецептов аудио.

### A2

- каждый набор имеет ровно одно животное с нужным trait.

### A3

- правильный ответ соответствует силуэту;
- конфликтующие животные не появляются вместе.

### N1

- распознавание числа поддерживает 1–20;
- визуальные количества ограничены 1–10;
- число отображаемых предметов совпадает с данными;
- аудиорецепты используют корректную форму числа и объекта.

### N2

- всегда два ответа;
- группы одного типа;
- корректно определяется больше/меньше.

### N3

- сумма ≤ 10;
- разность > 0;
- результат соответствует визуальному состоянию;
- один тип объекта внутри задания.

## 17.3. Проверка ресурсов

Автоматически проверять:

```text
53 мастер-ресурса в исходном реестре
44 PNG-мастера
9 SVG
187 публикуемых аудиоклипов: 175 после нарезки + 12 существующих реплик
```

Производные WebP и иконки проверяются отдельно как артефакты сборки и не увеличивают количество мастер-ресурсов.

---

# 18. Что намеренно не входит в эту итерацию

Не реализуются:

- `Әлифба`;
- `Табигать`;
- задания, требующие чтения;
- сопоставление картинки и написанного слова;
- первая буква слова;
- слоги;
- поиск лишнего животного;
- узнавание животного по голосу;
- сторонние аудиозаписи голосов животных;
- следующее/предыдущее число;
- пропущенное число;
- символические выражения `3 + 2 = ?`;
- знаки `<`, `>` и `=`;
- визуальные группы 11–20 как основная счётная задача;
- сохранённый игровой прогресс;
- аккаунты;
- очки;
- уровни, видимые ребёнку;
- рейтинги;
- обязательное прохождение разделов по порядку;
- отдельные PNG/SVG каждой цветовой или количественной комбинации.

---

# 19. Критерии готовности этой итерации

Итерация считается содержательно готовой к релизной проверке, когда:

1. реализованы два возрастных режима и переключение взрослым;
2. младший режим работает с 13 цветами, 39 животными и числами 1–10;
3. старший режим содержит все 10 механик C1–C4, A1–A3, N1–N3;
4. диапазон чисел 11–20 используется только там, где не требуется визуально считать 11–20 предметов;
5. подключены 43 контентных PNG через производные WebP, мастер иконки через четыре производные PNG-иконки и 9 локальных SVG;
6. удалённые `cube/top/pyramid` отсутствуют в обязательном реестре и офлайн-кэше;
7. аудиосервис поддерживает последовательности клипов и все рецепты финального сценария;
8. доступны все 187 публикуемых аудиоклипов, включая 12 интерактивных реплик;
9. все графические и аудиоресурсы входят в проверенный офлайн-набор;
10. автоматические тесты проверяют границы диапазонов, адаптивную сложность, уникальность ответов, арифметику, traits и конфликты силуэтов;
11. финальные склейки аудио прослушаны на реальном телефоне;
12. полный размер расширенного офлайн-набора измерен и отдельно подтверждён.

---

# 20. Итоговая продуктовая модель обновления

## Младший режим — 2–4 года

- три раздела;
- 13 цветов;
- 39 животных;
- числа 1–10;
- один смысловой тип упражнения: «услышать → найти»;
- адаптивные `2 → 3 → 4` варианта;
- несколько естественных аудиоформулировок одной задачи;
- без чтения;
- без сохраняемого игрового прогресса.

## Старший режим — 5–7 лет

- те же три раздела;
- те же 13 цветов;
- те же 39 животных;
- числа 1–20 для распознавания;
- визуальный счёт и визуальная арифметика преимущественно 1–10;
- 10 игровых механик;
- три внутренних уровня сложности;
- постепенное появление более сложных типов заданий;
- адаптивные `4 → 5 → 6` вариантов там, где это естественно;
- N2 остаётся двухвариантным;
- C3 использует 6 форм и два состава признаков: `цвет + форма` и `размер + цвет + форма`;
- C2 использует 3 утверждённых нейтральных SVG-предмета и при необходимости геометрические формы;
- A3 строит силуэт из существующей иллюстрации, без отдельных силуэтных файлов;
- визуальная арифметика выполняется без обязательных письменных выражений;
- без аккаунтов, рейтингов и обязательного прохождения.

## Финальные ресурсы

```text
53 мастер-графических ресурса
= 44 PNG + 9 SVG

5 WAV-мастеров
→ 175 коротких MP3-клипов
```

**Главный принцип этой итерации:** сложность создаётся более сложными операциями над общей проверенной базой визуального и озвученного татарского материала, а не чтением, накопительными наградами или разрастанием количества отдельных готовых ассетов.


# 21. Открытые задачи и порядок реализации

Это обязательный список незавершённых работ. Наличие файла или поля не означает языковую, визуальную или релизную приёмку.

| ID | Задача | Ожидаемый результат | Статус |
|---|---|---|---|
| V2-01 | Проанализировать признаки всех 39 животных для A2 | Вычитанная матрица `bird/domestic/wild/canFly/canSwim`, спорные случаи исключены, допустимые наборы имеют ровно один ответ и достаточно помех для 4–6 вариантов | Не выполнено |
| V2-02 | Проверить контуры всех 39 животных для A3 | Матрица конфликтующих пар/групп и допустимые наборы 4–6 вариантов; генератор проверен на невозможность построения набора | Не выполнено |
| V2-03 | Зафиксировать балансировку | Пороги `2 ↔ 3 ↔ 4` младшего режима, разрешённые подвиды числовых заданий младшего режима, правило серий ошибок/успехов и смешивания доступных механик; распределение для животных без L3 | Не выполнено |
| V2-04 | Проверить компоновку заданий и 4–6 ответов | Макеты для 320×568, 390×844, 768×1024, 1024×768, 844×390 с местом для стимула, safe area и установленными минимумами; если условия несовместимы — явное изменение ТЗ до реализации | Не выполнено |
| V2-05 | Согласовать интерактивную речь с новыми упражнениями | Все 12 существующих реплик используются; при необходимости отдельный список новых текстов, событий и Clip ID, без автоматического расширения 187 | Базовая политика сохранена; анализ дополнений не выполнен |
| V2-06 | Записать и проверить аудиоконструктор | Пробные склейки до массовой записи, пять WAV-мастеров, 175 нарезанных MP3, языковая проверка и прослушивание на телефоне | Ожидаются записи владельца |
| V2-07 | Подключить и принять v2 | Генерация производных, типы упражнений, режимы, аудиорецепты, миграция каталога и валидаторов, 187 MP3 в полном офлайн-наборе, тесты, измеренный бюджет и устройства | Не выполнено |

Следующий этап — V2-01–V2-04: анализ контента, допустимых наборов и компоновки. Затем реализация общей модели упражнения и последовательностей аудио, младшего режима и механик старшего режима по TDD. Подключение v2 в релиз требует полного аудионабора; ожидаемые файлы не заменяются заглушками и не пропускаются строгой проверкой.

# 22. Сохраняемые технические требования

[Спецификация MVP](specification-mvp.md) сохраняется как описание действующей версии и источник общих контрактов. Для v2 обязательны её разделы 3.4 (hash-навигация), 5 (визуальные токены и доступность), 7–8 (язык и стек), 9.1–9.2 (границы и структура), 10–18 (восстановление, аудио, PWA, публикация, безопасность, проверки и приёмка) с перечисленными ниже изменениями. Старые продуктовые ограничения и количества ресурсов не заменяют целевые значения настоящего ТЗ.

- Модель `Exercise`, режимы, адаптация, составной `AudioPrompt`, 13 цветов, 39 животных, диапазоны 1–10/1–20 и 187 аудиоклипов определяются настоящим документом.
- Старый запрет сборки фраз из слов относится только к MVP. Для v2 действуют готовые словоформы и рецепты раздела 15.
- Старые два ответа заменяются правилами каждой механики; N2 всегда имеет два ответа. Минимумы кнопок 56×56 и ответов 128×128, промежутки 16 px и отсутствие обычной прокрутки остаются требованиями до решения V2-04.
- Цвета интерфейса и учебные HEX разделены; нейтральные SVG используют `currentColor`. Имена производных иконок остаются `pwa-192x192.png`, `pwa-512x512.png`, `pwa-maskable-512x512.png`, `apple-touch-icon.png`.
- Правильный ответ принимается один раз; сохраняются 1200 мс после ответа и 300 мс после окончания подтверждения. Неверный ответ сохраняет упражнение и позиции, даёт реакцию 250 мс; после двух ошибок — статичная подсказка. Бездействие — один повтор через 10 секунд после полного задания. Уточнение C3 находится в разделе 13.5.
- Прямое распознавание сохраняет перемешанные циклы без повторения цели на стыке; правило распределения целей разных механик уточняется V2-03. Ошибки и устаревшие события отсеиваются по сессии, упражнению и операции.
- Скрытие страницы останавливает звук и таймеры; продолжение требует нажатия. Отмена всей аудиопоследовательности не считается её окончанием. Ошибка обязательного клипа или изображения блокирует ответы; загрузка ограничена 15 секундами и явным повтором. Ошибка необязательной в момент игры реплики не отменяет учебную подсказку; ожидание — до 2 секунд.
- PWA готова офлайн только после подтверждения полного набора текущей версии. Обновление ожидает закрытия старых окон, без `skipWaiting` и перезагрузки игры; сохраняется изоляция `tamchy` и проверка нескольких вкладок.
- Хранить можно только выбранный режим `junior | senior` с ключом префикса `tamchy`; результаты и адаптация остаются в памяти. Личные данные, аналитика, внешние сервисы и разрешения устройства не добавляются.
- Публикация остаётся через существующие GitHub Actions из проверенного артефакта. Команды проверки из раздела 17 MVP сохраняются; при подготовке данных проверяются только затронутые контракты. Порог покрытия домена — не ниже 90% строк и ветвей.
- T01–T18 сохраняются и расширяются на новые упражнения. T08 охватывает весь визуальный счёт 1–10; A01 — три раздела и 72 базовых элемента (13 цветов + 39 животных + 20 чисел), с возрастными диапазонами. К ним добавляются проверки раздела 17 и критерии раздела 19 настоящего ТЗ.
- Бюджеты JS/CSS/первого экрана/иллюстрации и показатели отзывчивости сохраняются. Полный пакет повторно измеряется после кодирования аудио и изображений: прежние 8 МиБ не объявляются автоматически подтверждёнными для v2.
- Финальную языковую, визуальную и звуковую приёмку выполняет владелец. Проверка на настоящих iPhone/Android и наблюдение за детьми не подменяются автоматическими проверками.
