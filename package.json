{
  "name": "tamchy",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": "24.20.0",
    "npm": "11.19.0"
  },
  "packageManager": "npm@11.19.0",
  "scripts": {
    "dev": "npm run assets:prepare && vite --host 127.0.0.1",
    "assets:prepare": "node scripts/prepare-assets.ts",
    "validate:catalog": "npm run assets:prepare && node scripts/validate-content.ts catalog",
    "validate:content": "npm run assets:prepare && node scripts/validate-content.ts content",
    "build": "npm run typecheck && npm run validate:content && vite build",
    "build:check": "npm run typecheck && npm run validate:catalog && vite build --mode check",
    "preview": "vite preview --host 127.0.0.1",
    "preview:check": "vite preview --mode check --host 127.0.0.1",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint . --max-warnings 0",
    "typecheck": "tsc -p tsconfig.game.json && tsc -p tsconfig.app.json && tsc -p tsconfig.node.json && tsc -p tsconfig.e2e.json",
    "test:unit": "vitest run --exclude 'tests/integration/**'",
    "test:integration": "vitest run tests/integration",
    "test:e2e": "node scripts/test-e2e.ts",
    "check:assets": "node scripts/check-assets.ts",
    "check:budgets": "node scripts/check-budgets.ts",
    "check:lighthouse": "node scripts/check-lighthouse.ts",
    "check:published": "node scripts/check-published.ts",
    "check:published:local": "node scripts/check-published-local.ts",
    "release:verify": "node scripts/verify-release.ts",
    "validate:expansion": "vitest run scripts/lib/expansion.test.ts --reporter=default --silent=false",
    "check:tint": "node scripts/check-tint.ts"
  },
  "dependencies": {
    "fast-png": "8.0.0",
    "react": "19.2.8",
    "react-dom": "19.2.8"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.63.0",
    "@testing-library/dom": "10.4.1",
    "@testing-library/jest-dom": "7.0.1",
    "@testing-library/react": "16.3.3",
    "@testing-library/user-event": "14.6.7",
    "@types/node": "24.13.3",
    "@types/react": "19.2.18",
    "@types/react-dom": "19.2.7",
    "@vitejs/plugin-react": "6.1.1",
    "@vitest/coverage-v8": "5.0.0",
    "eslint": "10.10.0",
    "eslint-plugin-react-hooks": "7.1.1",
    "jsdom": "30.0.1",
    "lighthouse": "13.4.1",
    "prettier": "3.9.6",
    "sharp": "0.35.4",
    "typescript": "6.0.3",
    "typescript-eslint": "8.69.0",
    "vite": "8.2.2",
    "vite-plugin-pwa": "1.3.0",
    "vitest": "5.0.0",
    "workbox-build": "7.4.1",
    "workbox-window": "7.4.1",
    "zod": "4.5.4"
  }
}
